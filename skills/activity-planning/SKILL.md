---
name: activity-planning
description: 当用户需要为社团、班级、校园组织或小型团队设计、补全或审查活动方案，包括流程、预算、排期、分工、宣传和风险预案时使用。
---

# 活动策划

把零散需求整理为可执行、可校验的活动方案。先取证和规划，再计算与校验；不要先写宣传文案。

## 执行流程

1. 提取需求
   - 识别活动类型、目标、对象、人数、日期或时段、时长、场地、预算上限和特殊约束。
   - 人数、预算上限、时长等决定方案可行性的必要信息缺失时，先询问用户。
   - 非关键项可采用保守假设，但必须在输出的“假设与待确认项”中逐条标明。

2. 查找历史资料
   - 用户提供文件或指定资料目录时，先用文件列表或 glob 找候选文件，再用 grep 搜活动类型、预算、人数和复盘关键词，最后只读取相关文件。
   - 提取可复用环节、真实价格、物资库存、参与率、问题和改进项，并注明来源文件。
   - 没有历史资料时继续执行，不得虚构历史数据或当地报价。

3. 设计活动骨架
   - 给出主题、目标、目标参与者、活动形式、核心环节、资源需求和 3 至 5 个可衡量成功指标。
   - 每个环节写清目的、时长、参与方式、所需物资和前置依赖。
   - 确保方案适配人数、场地、目标和安全约束。

4. 编制并校验预算
   - 将每项支出写为：名称、类别、单价、数量、小计、优先级、价格依据。
   - 优先调用 `calculate_budget`；若没有该工具，使用计算工具或脚本做确定性计算，不要心算关键金额。
   - 计算小计、分类合计、备用金、总额和余额。默认备用金比例为 10%，但应明确这是可调整假设。
   - 超预算时先削减可选项，再调整数量或寻找替代方案；每次调整后重新计算，不能只口头声称已达标。

5. 拆解任务与排期
   - 按前期筹备、活动当天、收尾复盘拆分任务；每项写负责人或角色、截止时间、交付物、依赖和备份人。
   - 优先调用 `build_schedule` 计算各环节起止时间；没有该工具时使用时间计算脚本或计算工具。
   - 安排签到、布场、撤场和缓冲时间，检查依赖环、时间窗口、人员冲突及无人负责的任务。

6. 生成宣传内容
   - 在方案、预算和时间确定后，再生成活动摘要、标题和宣传文案。
   - 文案必须包含时间、地点、对象、报名方式、截止时间和必要提醒；未知信息使用明确占位符，不得编造。

7. 风险与统一校验
   - 至少检查天气或场地、设备、人员缺席、安全、低参与度、预算波动和隐私风险；逐项给出触发条件、预防措施、应急动作和责任角色。
   - 优先调用 `validate_project`。发现错误时修改对应模块，重新运行预算或排期工具，再次校验，直到无硬性错误或明确说明无法消除的阻塞项。

8. 输出结果
   - 按下方结构输出。用户要求保存时，使用写文件工具写入指定路径，并在答复中报告路径。
   - 区分事实、来源数据和假设；金额统一币种，时间统一格式。

## 注意事项

- 不读取无关文件，不泄露成员电话、邮箱等个人信息。
- 不把估算价格写成真实报价；标注估价日期、地区或待询价状态。
- 不因生成了完整文本就跳过数学、时间、分工和风险校验。
- 涉及大型人群、未成年人、食品、医疗、消防或户外高风险活动时，明确提示审批和专业安全要求。

## 示例输出

```markdown
# 书画社春季交流活动方案

## 1. 需求摘要与假设
- 目标：促进社员交流
- 规模：40 人
- 约束：3 小时，预算不超过 1000 元
- 待确认：教室是否可使用投影设备

## 2. 主题、目标与成功指标
...

## 3. 活动流程
| 环节 | 开始 | 结束 | 目的 | 负责人角色 | 物资 |
|---|---:|---:|---|---|---|

## 4. 预算
| 项目 | 单价 | 数量 | 小计 | 优先级 | 依据 |
|---|---:|---:|---:|---|---|

小计：...；备用金：...；总计：...；余额：...

## 5. 任务与分工
| 任务 | 截止时间 | 负责人角色 | 备份人 | 交付物 | 依赖 |
|---|---|---|---|---|---|

## 6. 宣传文案
...

## 7. 风险预案
| 风险 | 触发条件 | 预防措施 | 应急动作 | 责任角色 |
|---|---|---|---|---|

## 8. 校验结果
- 预算：通过/不通过，依据...
- 排期：通过/不通过，依据...
- 分工：通过/不通过，依据...
- 内容完整性：通过/不通过，依据...
```